import {
  DataSource,
  ViewColumn,
  ViewEntity,
} from 'typeorm';

@ViewEntity({
  expression: (ds: DataSource) => {
    return ds.createQueryBuilder().select('*');
  },
})
export class <%= className %>View {
  @ViewColumn()
  name: string;
}
