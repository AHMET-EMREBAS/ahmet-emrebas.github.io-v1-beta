import {
  DataSource,
  ViewColumn,
  ViewEntity,
} from 'typeorm';

@ViewEntity({
  expression: (ds: DataSource) => {
    return ds.createQueryBuilder().select('*').from('<%= fileName %>', '<%= fileName %>');
  },
})
export class <%= className %>View {
 <% for(var i=0; i < columns.length; i++) { %>
  @ViewColumn()
  <%= columns[i].name %>: <%= columns[i].valueType %>;
<% } %>
}
