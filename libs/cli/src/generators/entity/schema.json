{
  "$schema": "http://json-schema.org/schema",
  "cli": "nx",
  "$id": "Entity",
  "title": "",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Entity Name",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What name would you like to use?"
    },
    "project": {
      "type": "string",
      "description": "Project Name",
      "$default": {
        "$source": "argv",
        "index": 1
      },
      "x-prompt": "Which project will you add the resource?"
    },
    "entity": {
      "type": "string",
      "description": "Entity schema object as JSON string",
      "x-prompt": "provide entity schema..."
    }
  },
  "required": ["name", "project"]
}
