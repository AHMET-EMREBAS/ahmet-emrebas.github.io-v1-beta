import { RepositoryService } from 'api-core';
import { <%= className %> } from 'models';
import { Repository } from 'typeorm';

import { Injectable } from '@nestjs/common';
import { InjectRepository } from '@nestjs/typeorm';

@Injectable()
export class <%= className %>Service extends RepositoryService<<%= className %>> {
  constructor(@InjectRepository(<%= className %>) <%= fileName %>Repository: Repository<<%= className %>>) {
    super(<%= fileName %>Repository);
  }
}
