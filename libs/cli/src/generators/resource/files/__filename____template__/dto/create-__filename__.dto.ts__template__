import { Property } from 'core';

export class Create<%= classname %>Dto {
  @Property({
    type: 'string',
    inputType: 'text',
    unique: true,
    minLength: 3,
    maxLength: 50,
  })
  sample: string;
}
