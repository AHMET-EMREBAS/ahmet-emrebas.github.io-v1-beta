import { SetAttributeDirective } from './set-attribute.directive';

describe('SetAttributeDirective', () => {
  it('should create an instance', () => {
    const directive = new SetAttributeDirective({} as any);
    expect(directive).toBeTruthy();
  });
});
