import {
  EntityMetadata,
  EntityPluralNames,
} from '@ngrx/data';

export const resourceEntityConfig: EntityMetadata = { entityName: 'Resource' };

export const resourcePluralNames: EntityPluralNames = {
  Resource: 'Resources',
};
