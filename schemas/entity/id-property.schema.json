{
  "$id": "IdPropertySchema",
  "title": "Id Property Schema",
  "description": "Property defination of an entity",
  "properties": {
    "name": {
      "type": "string",
      "description": "What is the property name?",
      "minLength": 1,
      "maxLength": 50,
      "default": "name"
    },
    "type": {
      "type": "string",
      "description": "What is the property type?",
      "enum": ["id"],
      "default": "id"
    },
    "label": {
      "type": "string",
      "description": "What is the visible property label?",
      "minLength": 1,
      "maxLength": 50,
      "default": "label"
    },
    "icon": {
      "type": "string",
      "description": "What is the property icon name?",
      "minLength": 1,
      "maxLength": 30,
      "default": "icon"
    },
    "inputElement": {
      "type": "string",
      "description": "Which HTML input element is used for the property?",
      "enum": ["number"]
    },
    "generated": {
      "type": "boolean",
      "description": "Is this field generated by the server or database?",
      "default": true
    }
  },
  "required": ["name", "type", "inputElement", "generated"],
  "additionalProperties": false
}
