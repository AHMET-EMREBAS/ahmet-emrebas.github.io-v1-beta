{
  "$id": "PasswordPropertySchema",
  "title": "Password  Property  Schema",
  "description": "Text Property  defination of an entity",
  "properties": {
    "name": {
      "type": "string",
      "description": "What is the property name?",
      "minLength": 1,
      "maxLength": 50,
      "default": "password"
    },
    "type": {
      "type": "string",
      "description": "What is the property type?",
      "enum": ["password"],
      "default": "password"
    },
    "label": {
      "type": "string",
      "description": "What is the visible property label?",
      "enum": ["Password", "New Password", "Old Password"],
      "minLength": 1,
      "maxLength": 50,
      "default": "Password"
    },
    "icon": {
      "type": "string",
      "description": "What is the property icon name?",
      "minLength": 1,
      "maxLength": 30,
      "default": "password"
    },
    "isPassword": {
      "type": "boolean",
      "enum": [true],
      "default": true
    },
    "minLength": {
      "type": "integer",
      "description": "What is the minimum length of the property?",
      "minimum": 6,
      "default": 6
    },
    "maxLength": {
      "type": "integer",
      "description": "What is the maximum length of the property?",
      "minimum": 50,
      "default": 50
    },
    "group": {
      "type": "string",
      "enum": ["Primary", "Secondary", "Others"],
      "description": "What is the property's input group? It is used for seperating inputs into different forms for better user experience",
      "minLength": 3,
      "maxLength": 50,
      "default": "Primary"
    },
    "query": {
      "type": "boolean",
      "description": "Is query operation allowed for the property?",
      "enum": [true],
      "default": true
    },
    "required": {
      "type": "boolean",
      "description": "Is the property required?",
      "default": false
    },
    "inputElement": {
      "type": "string",
      "description": "Which HTML input element is used for the property?",
      "enum": ["password"]
    },
    "description": {
      "type": "string",
      "description": "What is the role of this property?",
      "minLength": 1,
      "maxLength": 500,
      "default": "description"
    }
  },
  "required": [
    "name",
    "type",
    "inputElement",
    "isPassword",
    "minLength",
    "maxLength"
  ],
  "additionalProperties": false
}
